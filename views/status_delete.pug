extends layout

block content

  h1 Delete Status: #{status.status_name}
  
    p Do you really want to delete this Status?
    
    form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=status._id )

      button.btn.btn-primary(type='submit') Delete