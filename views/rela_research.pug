extends layout
block content
    h1 #{title}
    form(method='POST' action='')
        div.form-group
            label(for='telephone') User Telephone:
            input#telephone.form-control(type='text' placeholder='Telephone' name='telephone' 
                                            value=(undefined===telephone ? '' : telephone))
        button.btn.btn-primary(type='submit') 検索
    table(class="table table-striped table-hover")
        thead                
            th 名前
            th 性別 
            th 操作      
        tbody
            each user in all_users
                tr
                    td  #{user.name}
                    td  #{user.sex} 
                    td      
                        a(href=user.url_request_friend) 追加   
            else
                li There are no user.