extends layout

block content

    h1 Delete : #{user.user_name } #{user.birth} #{user.shelter.shelter_name}
        p Do you really want to delete this data?
    
        form(method='POST' action='')
            div.form-group
                input#id.form-control(type='hidden',name='id', required='true', value=user._id )
        
            button.btn.btn-primary(type='submit') Delete